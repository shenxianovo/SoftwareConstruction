<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HW03.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HW03"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="HW03">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel.DataContext>
            <local:ClockViewModel/>
        </StackPanel.DataContext>
        <StackPanel.Resources>
            <local:ClockFormatter x:Key="ClockFormatter"/>
        </StackPanel.Resources>

        <TextBlock Text="{Binding DefaultClockTime, Converter={StaticResource ClockFormatter}}"
                   FontSize="40"/>

        <StackPanel Orientation="Horizontal">
            <Button Content="Calibrate" Command="{Binding CalibrateCommand}"/>
        </StackPanel>

    </StackPanel>
</Window>
